# Проект запущен! 🚀

## Статус сервисов

### Backend (FastAPI)
- **URL**: http://localhost:8000
- **API документация**: http://localhost:8000/docs
- **Health check**: http://localhost:8000/health

### Frontend (React)
- **URL**: http://localhost:3000

## База данных

Используется SQLite (временно для быстрого запуска)
- Файл: `backend/kids_store.db`

Для перехода на PostgreSQL:
1. Установите и запустите PostgreSQL
2. Измените `DATABASE_URL` в `backend/app/core/config.py` на:
   ```
   DATABASE_URL = "postgresql://postgres:postgres@localhost:5432/kids_store"
   ```

## Использование

1. Откройте браузер: http://localhost:3000
2. Перейдите в раздел "Клиенты" и создайте тестового клиента
3. Создайте правила скидок в разделе "Скидки"
4. Обработайте покупку через "Касса"
5. Просмотрите аналитику в разделе "Аналитика"

## Остановка сервисов

Нажмите `Ctrl+C` в терминалах, где запущены серверы.

## Проблемы?

Если backend не запустился, проверьте:
- Python установлен и в PATH
- Все зависимости установлены: `pip install -r backend/requirements.txt`
- База данных создана (выполняется автоматически)

Если frontend не запустился:
- Node.js установлен
- Зависимости установлены: `npm install` в директории frontend

